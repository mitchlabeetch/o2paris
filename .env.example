# Database Connection (Neon PostgreSQL)
# Required for both development and production
DATABASE_URL=your_neon_postgresql_connection_string

# Admin Password Hash (hashed with bcrypt)
# Generate using: npm run generate-password YourPassword
# Default password in development (if not set): Admin123
# IMPORTANT: For Vercel deployment, set this environment variable as: ADMIN_PASSWORD_HASH
ADMIN_PASSWORD_HASH=your_bcrypt_hashed_password
