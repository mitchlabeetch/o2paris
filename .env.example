# Database Connection (Neon PostgreSQL)
# Required for both development and production
DATABASE_URL=your_neon_postgresql_connection_string

# Admin Password (plain text)
# Default password in development (if not set): Admin123
# IMPORTANT: For Vercel deployment, set this environment variable as: ADMIN_PASSWORD
# WARNING: Use a strong password in production. This should be kept secure through 
# Vercel's encrypted environment variables.
ADMIN_PASSWORD=your_admin_password
